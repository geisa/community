# SPDX-License-Identifier: Apache-2.0
#
# The pinned NXP 6.12 Cadence Kconfig allows DRM_CDNS_HDMI to call the CEC
# helpers without selecting the object that implements them. Keep the HDMI
# path enabled and build its CEC companion into the kernel.
CONFIG_DRM_CDNS_HDMI_CEC=y
